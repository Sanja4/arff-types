/**
 * The different attribute types in the ARFF file format. Used for mapping purposes.
 */
export enum ArffAttributeType {
    DATE = 'date',
    NUMERIC = 'numeric',
    STRING = 'string',
    ENUM = 'enum'
}
